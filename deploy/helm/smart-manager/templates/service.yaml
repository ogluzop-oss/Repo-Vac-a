apiVersion: v1
kind: Service
metadata:
  name: {{ include "smart-manager.fullname" . }}
  labels:
    {{- include "smart-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "smart-manager.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
