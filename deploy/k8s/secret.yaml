# PLANTILLA de secretos — NO contiene secretos reales (valores de ejemplo/placeholder).
# En producción, crea el Secret fuera de git (p. ej. `kubectl create secret generic ...`, Sealed
# Secrets, External Secrets o el Secret Manager del cloud). Los secretos NUNCA se versionan en claro.
apiVersion: v1
kind: Secret
metadata:
  name: smart-manager-secrets
  namespace: smart-manager
  labels:
    app.kubernetes.io/name: smart-manager
    app.kubernetes.io/component: backend
type: Opaque
stringData:
  DB_PASSWORD: "CHANGEME"
  SMART_MANAGER_JWT_SECRET: "CHANGEME"
  API_MASTER_KEY: "CHANGEME"
