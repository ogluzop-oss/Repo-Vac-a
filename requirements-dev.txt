# Dependencias de desarrollo (no necesarias para ejecutar la app)
-r requirements.txt
pytest==9.1.1
pytest-cov==7.1.0       # cobertura integrada en pytest
pytest-mock==3.15.1     # mocking ergonómico (fixture `mocker`)
pytest-rerunfailures==16.6  # reintenta tests flaky en CI (infra/timing); un fallo real falla en todos los intentos
freezegun==1.5.5        # congelar el tiempo en tests (fechas/replay)
coverage==7.15.4        # motor de cobertura
ruff==0.16.5
pyinstaller==6.22.2
lxml==6.1.2             # validación XSD de conformidad Verifactu (C3.3.1.1; dev/build)
